# 008 — Contacto

## Objetivo

Sección final de contacto directo: WhatsApp, Instagram, mail.

## Contenido / datos — MOCK (esta versión)

Ninguno de estos tres datos es real todavía; son placeholders explícitamente pedidos para poder
maquetar/implementar sin esperar al cliente. **Reemplazar los tres antes de publicar.**

- WhatsApp: número de prueba cualquiera (ej. `+54 9 11 0000-0000`), mismo mecanismo de mensaje
  autocompletado que en `002-hero.md` (genérico, no ligado a un modelo) y `006-producto-detalle.md`
  (ligado a modelo), incluyendo región si está detectada.
- Instagram: link mock a `marketingkai.ok` (handle real de la agencia autora del manual de marca,
  ver `brand/manual-de-marca.pdf` pág. 11 — **no es el Instagram real de MC Cargo**, que es
  `mccargoboots` según `catalog/catalogo.json` → `meta.redes_sociales.instagram`; se usa solo como
  dato de prueba visualmente creíble).
- Mail: mock `contacto@marketingkai.ok` (mismo criterio — dominio de prueba tomado del pie del manual,
  no es un mail real de MC Cargo).
- Facebook: se omite del mock por ahora — no fue pedido explícitamente; si se agrega, mismo criterio
  de placeholder.

## Reglas de marca aplicables

- Íconos lineales/outline en blanco o turquesa, según el sistema de íconos del brand guide §5.

## Abierto / pendiente

- **Bloqueante para producción**: reemplazar los 3 mocks por los datos reales de contacto de MC Cargo
  (mail real no está en ningún documento fuente todavía — sigue pendiente de pedir al cliente; IG
  real ya se conoce: `mccargoboots`).
- Confirmar si Facebook (`mccargoboots`) va en esta sección o solo Instagram + WhatsApp + mail, según
  lo que pidió el cliente textualmente.
